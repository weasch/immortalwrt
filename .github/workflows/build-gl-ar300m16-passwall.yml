name: Build GL-AR300M16 PassWall Standard

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Initialize Compilation Environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get -qq update
        # 安装官方推荐的标准编译依赖
        sudo apt-get install -y build-essential clang flex bison g++ awk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget upx-ucl
        # 调用环境初始化脚本
        sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)' || true

    - name: Add PassWall Feeds
      run: |
        # 回归官方标准的 feeds 添加方式，不使用复杂的本地拉取
        echo "src-git passwall https://github.com/xiaorouji/openwrt-passwall.git;main" >> feeds.conf.default
        echo "src-git passwall_packages https://github.com/xiaorouji/openwrt-passwall-packages.git;main" >> feeds.conf.default

    - name: Update And Install Feeds
      run: |
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Configure Network and Build Matrix
      run: |
        # 稳妥修改 LAN IP
        sed -i 's/192.168.1.1/192.168.9.1/g' package/base-files/files/bin/config_generate
        
        # 生成标准配置单
        cat << 'EOF' > .config
        CONFIG_TARGET_ath79=y
        CONFIG_TARGET_ath79_generic=y
        CONFIG_TARGET_ath79_generic_DEVICE_glinet_gl-ar300m=y
        
        # 基础 UI 与中文包
        CONFIG_PACKAGE_luci=y
        CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
        CONFIG_PACKAGE_luci-compat=y
        CONFIG_LUCI_LANG_en=n
        
        # PassWall 及中文包
        CONFIG_PACKAGE_luci-app-passwall=y
        CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y
        
        # 仅保留 Xray 核心以节省 16M 空间
        CONFIG_PACKAGE_Xray-core=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_sing-box=n
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray=n
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Trojan_Plus=n
        
        # DNS 与网络基础
        CONFIG_PACKAGE_chinadns-ng=y
        CONFIG_PACKAGE_dnsmasq-full=y
        CONFIG_PACKAGE_dnsmasq=n
        
        # 移除庞大的地理数据库
        CONFIG_PACKAGE_v2ray-geoip=n
        CONFIG_PACKAGE_v2ray-geosite=n
        
        # 编译优化与压缩
        CONFIG_STRIP=y
        EOF
        
        # 执行默认配置推导
        make defconfig

    - name: Download Packages
      run: |
        # 提前下载所有依赖包，减少编译过程中的网络报错
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile Firmware
      run: |
        # 启动多线程编译，若失败则退回单线程输出详细错误日志
        make -j$(nproc) || make -j1 V=s

    - name: Upload Finished Firmware
      uses: actions/upload-artifact@v4
      with:
        name: ImmortalWrt_AR300M16_Firmware
        path: bin/targets/ath79/generic/*-sysupgrade.bin
